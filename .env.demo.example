# .env.demo.example
# Copy to .env.demo and fill in real values. NEVER commit .env.demo.

# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace_me_with_a_48_byte_random_string

# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(24))"
POSTGRES_PASSWORD=replace_me_with_a_24_byte_random_string
